package algorithmsAndDS;

/**
 * 自底向上的归并排序
 */
public class MergeDownToUp {

    private static Comparable[] aux;

    private static void sort(Comparable[] a){

        int N = a.length;
        aux = new Comparable[N];   // 确定数组空间
        for (int i = 0; i < N; i += i) {  // 确定每一层的数组大小i;
        }
    }
}
